{
  "name": "centro-de-belleza-server",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side dependencies for the Centro de Belleza application.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.44.4",
    "@upstash/ratelimit": "2.0.6",
    "@upstash/redis": "^1.34.3",
    "resend": "3.5.0"
  },
  "devDependencies": {
    "@vercel/node": "5.3.8"
  }
}